#include <iostream>
#include <string>

using namespace std;

class Solution{
public:
	int strStr(const char* source, const char *target){
		//todo

		// cout << source.size() << end;
		// cout << target.size() << end;

		return -1;
	}
};


int main(){

	Solution sol;

	const char* source = "source";
	const char* target = "target";	

	sol.strStr(source, target);

	return 0;
}